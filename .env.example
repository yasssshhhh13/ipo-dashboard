# Copy this file's contents into Vercel → Project → Settings → Environment Variables.
# Never commit a real key into this file or anywhere in the repo.

# Required for the AI Assistant (used only by api/chat.js, server-side — never
# sent to the browser). Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
